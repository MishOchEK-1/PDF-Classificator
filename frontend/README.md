# Legacy Frontend Placeholder

The project no longer plans to use React.

The active UI direction is now:

- Django template files in `templates/`
- static CSS in `static/css/`
- static JavaScript in `static/js/`

This directory can be removed later if it stays unused.
